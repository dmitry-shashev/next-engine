import React from 'react'
import { render } from '@testing-library/react'
import { textInTheDocument } from '@tests/utils'
import {{ pascalCase componentName }} from './{{ pascalCase componentName }}'

describe('{{ pascalCase componentName }}', () => {
  it('component', async () => {
    const TEST_NAME = 'One'
    render(<{{ pascalCase componentName }} name={TEST_NAME} />)
    await textInTheDocument(TEST_NAME)
  })
})

